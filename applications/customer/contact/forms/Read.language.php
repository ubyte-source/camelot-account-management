<?php

// START - ENG ==========================================================


namespace language\en\applications\customer\contact\forms\read\field {
    const contact_name = 'Customer name';
    const contact_vat = 'VAT';
    const contact_address = 'Address';
    const contact_city = 'City';
    const contact_zip = 'Zip';
    const contact_province = 'Province';
    const contact_address_number = 'Address Number';
    const contact_address_number_type = 'Address Number Type';
    const contact_country = 'Country';
    const contact_note = 'Note';
    const contact_domain = 'Customer sites';
}

namespace language\en\applications\customer\contact\forms\read\field\contact_address_number_type {
    const civic = 'Civic';
    const km = 'Kilometers';
}

namespace language\en\applications\customer\contact\forms\read\field\contact_country {
    define(__namespace__ . '\\' . 'it', 'Italy');
    define(__namespace__ . '\\' . 'be', 'Belgium');
    define(__namespace__ . '\\' . 'nl', 'Netherlands');
    define(__namespace__ . '\\' . 'es', 'Spain');
    define(__namespace__ . '\\' . 'fi', 'Finland');
    define(__namespace__ . '\\' . 'si', 'Slovenia');
    define(__namespace__ . '\\' . 'ch', 'Switzerland');
    define(__namespace__ . '\\' . 'at', 'Austria');
    define(__namespace__ . '\\' . 'se', 'Sweden');
    define(__namespace__ . '\\' . 'pl', 'Poland');
    define(__namespace__ . '\\' . 'il', 'Israel');
    define(__namespace__ . '\\' . 'tr', 'Turkey');
    define(__namespace__ . '\\' . 'fr', 'France');
    define(__namespace__ . '\\' . 'ee', 'Estonia');
    define(__namespace__ . '\\' . 'ro', 'Romania');
    define(__namespace__ . '\\' . 'sk', 'Slovakia');
    define(__namespace__ . '\\' . 'lv', 'Latvia');
    define(__namespace__ . '\\' . 'in', 'India');
    define(__namespace__ . '\\' . 'ir', 'Iran');
    define(__namespace__ . '\\' . 'jo', 'Jordan');
    define(__namespace__ . '\\' . 'pt', 'Portugal');
    define(__namespace__ . '\\' . 'lt', 'Lithuania');
    define(__namespace__ . '\\' . 'az', 'Azerbaijan');
    define(__namespace__ . '\\' . 'us', 'United States of America');
    define(__namespace__ . '\\' . 'li', 'Liechtenstein');
    define(__namespace__ . '\\' . 'jp', 'Japan');
    define(__namespace__ . '\\' . 'gi', 'Gibraltar');
    define(__namespace__ . '\\' . 'ph', 'Philippines');
    define(__namespace__ . '\\' . 'cn', 'China');
    define(__namespace__ . '\\' . 'dz', 'Algeria');
    define(__namespace__ . '\\' . 'kz', 'Kazakhstan');
    define(__namespace__ . '\\' . 'id', 'Indonesia');
    define(__namespace__ . '\\' . 'im', 'Isle of Man');
    define(__namespace__ . '\\' . 'sg', 'Singapore');
    define(__namespace__ . '\\' . 'kw', 'Kuwait');
    define(__namespace__ . '\\' . 'hr', 'Croatia');
    define(__namespace__ . '\\' . 'gb', 'United Kingdom');
    define(__namespace__ . '\\' . 'ma', 'Morocco');
    define(__namespace__ . '\\' . 'fo', 'Faroe Islands');
    define(__namespace__ . '\\' . 'cz', 'Czech Republic');
    define(__namespace__ . '\\' . 'ie', 'Ireland');
    define(__namespace__ . '\\' . 'qa', 'Qatar');
    define(__namespace__ . '\\' . 'my', 'Malaysia');
    define(__namespace__ . '\\' . 'ae', 'United Arab Emirates');
    define(__namespace__ . '\\' . 'am', 'Armenia');
    define(__namespace__ . '\\' . 'rs', 'Serbia');
    define(__namespace__ . '\\' . 'sm', 'San Marino');
    define(__namespace__ . '\\' . 'no', 'Norway');
    define(__namespace__ . '\\' . 'mk', 'Macedonia');
    define(__namespace__ . '\\' . 'hk', 'Hong Kong');
    define(__namespace__ . '\\' . 'lk', 'Sri Lanka');
    define(__namespace__ . '\\' . 'vn', 'Viet Nam');
    define(__namespace__ . '\\' . 'br', 'Brazil');
    define(__namespace__ . '\\' . 'va', 'Vatican City State');
    define(__namespace__ . '\\' . 'by', 'Belarus');
    define(__namespace__ . '\\' . 'jm', 'Jamaica');
    define(__namespace__ . '\\' . 'ar', 'Argentina');
    define(__namespace__ . '\\' . 'ge', 'Georgia');
    define(__namespace__ . '\\' . 'ad', 'Andorra');
    define(__namespace__ . '\\' . 'py', 'Paraguay');
    define(__namespace__ . '\\' . 'pr', 'Puerto Rico');
    define(__namespace__ . '\\' . 'kr', 'Korea (South)');
    define(__namespace__ . '\\' . 'mx', 'Mexico');
    define(__namespace__ . '\\' . 'cl', 'Chile');
    define(__namespace__ . '\\' . 'md', 'Moldova');
    define(__namespace__ . '\\' . 'au', 'Australia');
    define(__namespace__ . '\\' . 'mt', 'Malta');
    define(__namespace__ . '\\' . 'de', 'Germany');
    define(__namespace__ . '\\' . 'th', 'Thailandia');
    define(__namespace__ . '\\' . 'cy', 'Cyprus');
    define(__namespace__ . '\\' . 'za', 'South Africa');
    define(__namespace__ . '\\' . 'mm', 'Myanmar');
    define(__namespace__ . '\\' . 'sa', 'Saudi Arabia');
    define(__namespace__ . '\\' . 'ba', 'Bosnia and Herzegovina');
    define(__namespace__ . '\\' . 'np', 'Nepal');
    define(__namespace__ . '\\' . 'ua', 'Ukraine');
    define(__namespace__ . '\\' . 'sz', 'Swaziland');
    define(__namespace__ . '\\' . 'dk', 'Denmark');
    define(__namespace__ . '\\' . 'lu', 'Luxembourg');
    define(__namespace__ . '\\' . 'al', 'Albania');
    define(__namespace__ . '\\' . 'tw', 'Taiwan');
    define(__namespace__ . '\\' . 'bg', 'Bulgaria');
    define(__namespace__ . '\\' . 'mc', 'Monaco');
    define(__namespace__ . '\\' . 'pa', 'Panama');
    define(__namespace__ . '\\' . 'tn', 'Tunisia');
    define(__namespace__ . '\\' . 'ca', 'Canada');
    define(__namespace__ . '\\' . 'eg', 'Egypt');
    define(__namespace__ . '\\' . 'nz', 'New Zealand');
    define(__namespace__ . '\\' . 'om', 'Oman');
    define(__namespace__ . '\\' . 'ly', 'Libya');
    define(__namespace__ . '\\' . 'is', 'Iceland');
    define(__namespace__ . '\\' . 'gr', 'Greece');
    define(__namespace__ . '\\' . 'hu', 'Hungary');
    define(__namespace__ . '\\' . 'ru', 'Russian Federation');
    define(__namespace__ . '\\' . 'me', 'Montenegro');
    define(__namespace__ . '\\' . 'xk', 'Kosovo'); 
}

// END - ENG ==========================================================

// START - ITA ========================================================


namespace language\it\applications\customer\contact\forms\read\field {
    const contact_name = 'Nome Cliente';
    const contact_vat = 'VAT';
    const contact_address = 'Indirizzo';
    const contact_city = 'Città';
    const contact_zip = 'CAP';
    const contact_province = 'Provincia';
    const contact_address_number = 'Numero indirizzo';
    const contact_address_number_type = 'Tipologia del numero';
    const contact_country = 'Nazione';
    const contact_note = 'Note';
    const contact_domain = 'Siti del cliente';
}

namespace language\it\applications\customer\contact\forms\read\field\contact_address_number_type {
    const civic = 'Civico';
    const km = 'Kilometro';
}

namespace language\it\applications\customer\contact\forms\read\field\contact_country {
    define(__namespace__ . '\\' . 'it', 'Italia');
    define(__namespace__ . '\\' . 'be', 'Belgio');
    define(__namespace__ . '\\' . 'nl', 'Paesi Bassi');
    define(__namespace__ . '\\' . 'es', 'Spagna');
    define(__namespace__ . '\\' . 'fi', 'Finlandia');
    define(__namespace__ . '\\' . 'si', 'Slovenia');
    define(__namespace__ . '\\' . 'ch', 'Svizzera');
    define(__namespace__ . '\\' . 'at', 'Austria');
    define(__namespace__ . '\\' . 'se', 'Svezia');
    define(__namespace__ . '\\' . 'pl', 'Polonia');
    define(__namespace__ . '\\' . 'il', 'Israele');
    define(__namespace__ . '\\' . 'tr', 'Turchia');
    define(__namespace__ . '\\' . 'fr', 'Francia');
    define(__namespace__ . '\\' . 'ee', 'Estonia');
    define(__namespace__ . '\\' . 'ro', 'Romania');
    define(__namespace__ . '\\' . 'sk', 'Slovacchia');
    define(__namespace__ . '\\' . 'lv', 'Lettonia');
    define(__namespace__ . '\\' . 'in', 'India');
    define(__namespace__ . '\\' . 'ir', 'Iran');
    define(__namespace__ . '\\' . 'jo', 'Giordania');
    define(__namespace__ . '\\' . 'pt', 'Portogallo');
    define(__namespace__ . '\\' . 'lt', 'Lituania');
    define(__namespace__ . '\\' . 'az', 'Azerbaijan');
    define(__namespace__ . '\\' . 'us', 'Stati Uniti d America');
    define(__namespace__ . '\\' . 'li', 'Liechtenstein');
    define(__namespace__ . '\\' . 'jp', 'Giappone');
    define(__namespace__ . '\\' . 'gi', 'Gibilterra');
    define(__namespace__ . '\\' . 'ph', 'Filippine');
    define(__namespace__ . '\\' . 'cn', 'Cina');
    define(__namespace__ . '\\' . 'dz', 'Algeria');
    define(__namespace__ . '\\' . 'kz', 'Kazakhstan');
    define(__namespace__ . '\\' . 'id', 'Indonesia');
    define(__namespace__ . '\\' . 'im', 'Isola di Man');
    define(__namespace__ . '\\' . 'sg', 'Singapore');
    define(__namespace__ . '\\' . 'kw', 'Kuwait');
    define(__namespace__ . '\\' . 'hr', 'Croazia');
    define(__namespace__ . '\\' . 'gb', 'Regno Unito');
    define(__namespace__ . '\\' . 'ma', 'Marocco');
    define(__namespace__ . '\\' . 'fo', 'Isole Faroe');
    define(__namespace__ . '\\' . 'cz', 'Repubblica Ceca');
    define(__namespace__ . '\\' . 'ie', 'Irlanda');
    define(__namespace__ . '\\' . 'qa', 'Qatar');
    define(__namespace__ . '\\' . 'my', 'Malesia');
    define(__namespace__ . '\\' . 'ae', 'Emirati Arabi Uniti');
    define(__namespace__ . '\\' . 'am', 'Armenia');
    define(__namespace__ . '\\' . 'rs', 'Serbia');
    define(__namespace__ . '\\' . 'sm', 'San Marino');
    define(__namespace__ . '\\' . 'no', 'Norvegia');
    define(__namespace__ . '\\' . 'mk', 'Macedonia');
    define(__namespace__ . '\\' . 'hk', 'Hong Kong');
    define(__namespace__ . '\\' . 'lk', 'Sri Lanka');
    define(__namespace__ . '\\' . 'vn', 'Viet Nam');
    define(__namespace__ . '\\' . 'br', 'Brasile');
    define(__namespace__ . '\\' . 'va', 'Città del Vaticano');
    define(__namespace__ . '\\' . 'by', 'Bielorussia');
    define(__namespace__ . '\\' . 'jm', 'Giamaica');
    define(__namespace__ . '\\' . 'ar', 'Argentina');
    define(__namespace__ . '\\' . 'ge', 'Georgia');
    define(__namespace__ . '\\' . 'ad', 'Andorra');
    define(__namespace__ . '\\' . 'py', 'Paraguay');
    define(__namespace__ . '\\' . 'pr', 'Puerto Rico');
    define(__namespace__ . '\\' . 'kr', 'Korea (Sud)');
    define(__namespace__ . '\\' . 'mx', 'Messico');
    define(__namespace__ . '\\' . 'cl', 'Cile');
    define(__namespace__ . '\\' . 'md', 'Moldavia');
    define(__namespace__ . '\\' . 'au', 'Australia');
    define(__namespace__ . '\\' . 'mt', 'Malta');
    define(__namespace__ . '\\' . 'de', 'Germania');
    define(__namespace__ . '\\' . 'th', 'Tailandia');
    define(__namespace__ . '\\' . 'cy', 'Cipro');
    define(__namespace__ . '\\' . 'za', 'Sud Africa');
    define(__namespace__ . '\\' . 'mm', 'Birmania');
    define(__namespace__ . '\\' . 'sa', 'Arabia Saudita');
    define(__namespace__ . '\\' . 'ba', 'Bosnia ed Erzegovina');
    define(__namespace__ . '\\' . 'np', 'Nepal');
    define(__namespace__ . '\\' . 'ua', 'Ucraina');
    define(__namespace__ . '\\' . 'sz', 'Swaziland');
    define(__namespace__ . '\\' . 'dk', 'Danimarca');
    define(__namespace__ . '\\' . 'lu', 'Lussemburgo');
    define(__namespace__ . '\\' . 'al', 'Albania');
    define(__namespace__ . '\\' . 'tw', 'Taiwan');
    define(__namespace__ . '\\' . 'bg', 'Bulgaria');
    define(__namespace__ . '\\' . 'mc', 'Monaco');
    define(__namespace__ . '\\' . 'pa', 'Panama');
    define(__namespace__ . '\\' . 'tn', 'Tunisia');
    define(__namespace__ . '\\' . 'ca', 'Canada');
    define(__namespace__ . '\\' . 'eg', 'Egitto');
    define(__namespace__ . '\\' . 'nz', 'New Zealand');
    define(__namespace__ . '\\' . 'om', 'Oman');
    define(__namespace__ . '\\' . 'ly', 'Libia');
    define(__namespace__ . '\\' . 'is', 'Islanda');
    define(__namespace__ . '\\' . 'gr', 'Grecia');
    define(__namespace__ . '\\' . 'hu', 'Ungheria');
    define(__namespace__ . '\\' . 'ru', 'Federazione Russa');
    define(__namespace__ . '\\' . 'me', 'Montenegro');
    define(__namespace__ . '\\' . 'xk', 'Kosovo');
}

// END - ITA ==========================================================
